const controller = require('./upload.controller.js');
const route = require('./upload.route.js');
const service = require('./upload.service.js');

module.exports = {
  controller,
  route,
  service,
};
